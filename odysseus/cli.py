"""The ``odysseus`` command line.

A placeholder: it names itself and points at the Python API. Real argument
handling (task, ``--resume``, ``--workdir``) lands in a later change.
"""


def main(argv=None):
    """Entry point for ``python -m odysseus``; not yet implemented."""
    print("odysseus: the command line is not built yet. "
          "For now: `from odysseus import Harness`.")
